# শাহজাহান চৌধুরী - অফিশিয়াল ওয়েবসাইট
## MP Shahjahan Chowdhury - Official Website

---

## 🚀 প্রজেক্ট সেটআপ গাইড (বাংলায়)

### ধাপ ১: Node.js ইনস্টল করুন
👉 https://nodejs.org/ থেকে **LTS** version ডাউনলোড করুন

### ধাপ ২: এই ফোল্ডারে প্রজেক্ট তৈরি করুন

আপনার কম্পিউটারে যেকোনো জায়গায় একটি ফোল্ডার তৈরি করুন, যেমন: `C:\Projects\shahjahan-website`

এই প্রজেক্টের সব ফাইল সেই ফোল্ডারে রাখুন।

### ধাপ ৩: Terminal/Command Prompt খুলুন

VS Code-এ: View → Terminal → New Terminal

তারপর প্রজেক্ট ফোল্ডারে যান:
```bash
cd C:\Projects\shahjahan-website
```

### ধাপ ৪: Dependencies ইনস্টল করুন
```bash
npm install
```

### ধাপ ৫: Development Server চালু করুন
```bash
npm run dev
```

তারপর browser-এ যান: **http://localhost:5173**

---

## 📁 প্রজেক্ট স্ট্রাকচার

```
shahjahan-website/
├── src/
│   ├── components/         ← Navbar, Footer, PageHero
│   ├── pages/              ← প্রতিটি পেজের ফাইল
│   │   ├── Home.jsx + Home.css
│   │   ├── About.jsx + About.css
│   │   ├── Activities.jsx + Activities.css
│   │   ├── SocialWork.jsx + SocialWork.css
│   │   ├── Gallery.jsx + Gallery.css
│   │   ├── Constituency.jsx + Constituency.css
│   │   ├── VoterSlip.jsx + VoterSlip.css
│   │   ├── News.jsx + News.css
│   │   └── Contact.jsx + Contact.css
│   ├── data/
│   │   └── staticData.js   ← ← ← সমস্ত তথ্য এখানে পরিবর্তন করুন
│   ├── App.jsx             ← Routes
│   └── index.css           ← Global styles & design system
├── index.html
├── package.json
└── vite.config.js
```

---

## ✏️ তথ্য পরিবর্তন করুন

**`src/data/staticData.js`** ফাইলটি খুলুন।

এখানে সব তথ্য আছে:
- `MP` → ব্যক্তিগত তথ্য (নাম, ফোন, ছবি, ইত্যাদি)
- `STATS` → হোমপেজের সংখ্যা
- `NEWS` → সংবাদ তালিকা
- `ACTIVITIES` → কার্যক্রম
- `SOCIAL_WORKS` → সামাজিক কাজের তথ্য
- `GALLERY` → গ্যালারি ছবি
- `VOTER_DATA` → ভোটার তথ্য (পরীক্ষামূলক)

### ছবি যোগ করুন:
1. `public/images/` ফোল্ডার তৈরি করুন
2. ছবি রাখুন যেমন `mp-main.jpg`
3. staticData.js-এ পরিবর্তন করুন:
   ```js
   photo: '/images/mp-main.jpg',
   ```

---

## 🎨 ডিজাইন কাস্টমাইজ

`src/index.css` ফাইলের `:root` সেকশনে রঙ পরিবর্তন করুন:
```css
:root {
  --forest: #0d3b2e;    /* মূল সবুজ */
  --gold: #c9a84c;      /* সোনালি */
  --cream: #f8f4ec;     /* ক্রিম */
}
```

---

## 🏗️ Production Build

```bash
npm run build
```
এটি `dist/` ফোল্ডারে ফাইল তৈরি করবে।

---

## 📱 পেজ তালিকা

| Route | পেজ |
|-------|-----|
| `/` | হোম |
| `/about` | পরিচিতি |
| `/activities` | কার্যক্রম |
| `/social-work` | সামাজিক কার্যক্রম |
| `/gallery` | গ্যালারি |
| `/constituency` | নির্বাচনী এলাকা |
| `/voter-slip` | ভোটার স্লিপ |
| `/news` | সংবাদ |
| `/contact` | যোগাযোগ |

---

## 🔧 সমস্যা হলে

**Error: Cannot find module** → `npm install` আবার চালান

**Port already in use** → `npm run dev -- --port 3000`

**Fonts লোড হচ্ছে না** → Internet connection চেক করুন (Google Fonts)
